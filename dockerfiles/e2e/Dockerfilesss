FROM selenium/standalone-chrome

USER root

ADD https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb /usr/bin

RUN apt install -y /usr/bin/google-chrome-stable_current_amd64.deb


# RUN apt-get install -y xorg \
#     openbox \
#     git \
#     unzip \
#     jq \
#     nmap \
#     nodejs && \
    # npm install -g typescript && \
    # apt install -y /usr/bin/google-chrome-stable_current_amd64.deb

# RUN rm /var/lib/dpkg/lock && \
#     dpkg --configure -a

# # COPY e2e/package.json e2e/package-lock.json /root/e2e/

# RUN cd /root/e2e && \
#     npm --silent i

# COPY e2e /root/e2e
# COPY docker-entrypoint.sh /root/

WORKDIR /root/

